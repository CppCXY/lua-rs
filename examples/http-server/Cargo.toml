[package]
name = "http-server"
version = "0.1.0"
edition = "2024"

[dependencies]
luars = { version = "0.22.0", features = ["sandbox"], workspace = true }
tokio = { version = "1", features = ["full"] }
