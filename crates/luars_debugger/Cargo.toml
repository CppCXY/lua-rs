[package]
name = "luars_debugger"
version = "0.1.0"
edition = "2024"
description = "Built-in DAP debugger for luars, loaded via require('emmy_core')"

[dependencies]
luars.workspace = true
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
